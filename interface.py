
from imsurance import Insurance

# from collections import NamedTuple


class Interface:
    # insured = []

    def __init__(self, name, surname, age, telefon ):

interface = Interface()

    #     self.record = [name, surname, age, telefon ]
    #
    #     RecordInsured()
        #
        # self.record =           RecordInsured()
    @staticmethod
    def create_insured(self):
        interface.name = input("Zadejte jmeno pojištěnce")
        interface.surname = input("Zadejte příjmení pojištěnce")
        interface.age = input("Zadejte věk pojištěnce")
        interface.telefon = input("Zadejte telefon pojištěnce")
        Insurance(name, surname, age, telefon)
        interface.append_insured(insurance)
